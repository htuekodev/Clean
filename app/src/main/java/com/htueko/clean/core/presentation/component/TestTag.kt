package com.htueko.clean.core.presentation.component

/**
 * to centralise the test tag for ui components
 */
object TestTag {
    /**
     * to centralise the test tag of all the button related components
     */
    object ButtonTag {
        const val cleanOutlineButtonTag = "cleanOutlineButtonTag"
    }

    /**
     * to centralise the test tag of all the Icon related components
     */
    object IconTag {
        const val cleanOutlineButtonIconTag = "cleanOutlineButtonIconTag"
    }

    /**
     * to centralise the test tag of all the Text related components
     */
    object TextTag {
        const val cleanOutlineButtonTextTag = "cleanOutlineButtonTextTag"
    }

    /**
     * to centralise the test tag of all the input text field related components
     */
    object TextFieldTag {
        const val cleanTextFieldParentTag = "cleanTextFieldParentTag"
        const val cleanTextFieldTag = "cleanTextFieldTag"
    }

    /**
     * to centralise the test tag of all the util related components
     */
    object UtilTag {
        const val cleanProgressIndicatorTag = "cleanProgressIndicatorTag"
        const val cleanVerticalSpacerTag = "cleanVerticalSpacerTag"
    }


}